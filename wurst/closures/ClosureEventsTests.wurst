package ClosureEventsTests
import ClosureEvents

@Test function testEvents()
	let hero = createUnit(players[0], 'hfoo' , ZERO2, 0 .fromDeg())
	EventListener.onCast(hero, 'Abld') (caster) ->
		print("OnCast")
