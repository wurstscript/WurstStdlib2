package MapBoundsTests
import MapBounds

@Test function testMapBounds()
	playableMin.x.assertEquals(-1024)
	playableMax.x.assertEquals(1024)
	playableMin.y.assertEquals(-1024)
	playableMax.y.assertEquals(1024)
	(playableMin + playableMax).toString().assertEquals(playableCenter.toString())
